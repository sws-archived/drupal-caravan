---
- hosts: se3_blt
  connection: docker
  gather_facts: no
  become: yes
  remote-user: root

  vars_files:
    - ../personal_vars.yml

  roles:
    - run-drupalvm
    - setup-acquia
    - install-site
    - setup-behat
    - run-behat
