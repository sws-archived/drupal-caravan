<?php
/**
 * @file
 * Local drush configuration for this project.
 *
 * This will be copied to local.drushrc.php by BLT when `blt setup`
 * or `blt local:setup` is run. Placeholders, like
 * http://earth.local, will be replaced in the target file.
 */

$options['uri'] = 'http://{{ sitename }}.local:{{ localhost_http_port }}';
